{
  "version": 2,
  "buildCommand": "npm run build",
  "builds": [
    {
      "src": "*",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/server/(.*)",
      "dest": "server/dist/main.js"
    }
  ]
}
