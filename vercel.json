{
  "version": 2,
  "buildCommand": "npm run build",
  "routes": [
    {
      "src": "/server/(.*)",
      "dest": "server/dist/main.js"
    }
  ],
  "functions": {
    "/server/(.*)": {
      "runtime": "@vercel/node"
    }
  }
}
